<form action="/problem/submitProblem" method="POST" enctype="multipart/form-data">
    <input type="text" placeholder="title" name="problem_title">
    <textarea placeholder="title" name="problem_desc"></textarea>
    <input type="hidden" placeholder="title" name="is_posted_anonymously" value="true">
    <select name="region"> <% for (let r of region) { %>
        <option value="<%= r %>"> <%= r %> </option>
    <% } %> </select>

    <select name="category"> <% for (let c of category) { %>
        <option value="<%= c %>"> <%= c %> </option>
    <% } %> </select>

    <input type="file" name="images" multiple>upload image</input>

    <input type="submit" value="submit" ></input>

</form>